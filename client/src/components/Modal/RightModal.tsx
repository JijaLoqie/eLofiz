import type { FC } from "react";


interface RightModalProps {
}

export const RightModal: FC<RightModalProps> = ({}) => {
    return (
        <div
            className={`
            sticky h-[96vh] w-[30vw] bg-orange-600/20 rounded backdrop-blur-sm mr-[4vw]
            z-20`}
        >
            asdasd
        </div>
    )
}